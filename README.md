# AtivaSeg — site

Página provisória ("em construção") da AtivaSeg Sistemas Integrados de Segurança, em Curitiba/PR.

- `index.html` — página única, autocontida (fontes Archivo e SVGs da marca embutidos em base64, sem dependências externas).
- Domínio de produção: **ativasegsis.com.br** (hospeda esta página temporária).
- Domínio de testes do site novo (completo): **ativaseg.com**.

## Deploy

Publicado via Cloudflare Pages, apontando para este repositório (branch `main`, build vazio — é HTML estático na raiz).
